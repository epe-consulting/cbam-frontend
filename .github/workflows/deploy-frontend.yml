name: Build and push frontend image

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to ACR
        uses: docker/login-action@v3
        with:
          registry: cbaregistry.azurecr.io
          username: ${{ secrets.ACR_USERNAME }}
          password: ${{ secrets.ACR_PASSWORD }}

      - name: Build and push frontend image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          build-args: |
            VITE_API_BASE_URL=${{ secrets.BACKEND_API_URL || 'https://cbam-prod.wittydune-4f9c0a93.spaincentral.azurecontainerapps.io' }}
          tags: |
            cbaregistry.azurecr.io/cbam-front-prod:${{ github.sha }}
            cbaregistry.azurecr.io/cbam-front-prod:latest
