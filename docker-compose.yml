services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cbam-frontend
    ports:
      - "5173:5173"
    restart: unless-stopped
    networks:
      - cbam-network

networks:
  cbam-network:
    driver: bridge
